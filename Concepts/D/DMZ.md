Network segment between the internet and the internal private network, typically with firewalls on both sides. It hosts publicly accessible services (web servers, mail servers, DNS servers) that need to be reachable from the internet while protecting the internal network.